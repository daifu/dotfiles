At first, let's modify the .bash_profile and .bashrc in the directory: ~/
Use your favor text editor to edit, for example, I use mvim to edit them, so
$ mvim ~/.bashrc ~/.bash_profile

Edit .bashrc:
. ~/bin/dotfiles/bashrc

Edit .bash_profile:
if [ -f ~/.bashrc ];
then
   source ~/.bashrc
fi
