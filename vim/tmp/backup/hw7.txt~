Not urgent to Urgent
0 to 5

HW7
SSH CRC32 attack detection code contains remote integer overflow
5, Urgent, because this vulnerability allows a remote attacker to 
execute arbitrary code with the privileges of the SSH daemon, typically root.
The most danger thing will be the lost of all the files and the damage of
the system.

Java Deployment Toolkit insufficient argument validation
4, Kinds of urgent, By convincing a user to visit a specially 
crafted HTML document, a remote attacker may be able to execute arbitrary 
code on a vulnerable system. The system will be damage.

Mozilla WOFF decoder integer overflow
3, Kinds of urgent, because a remote, unauthenticated attack may be able to 
execute arbitrary code with the privileges of the victim user. It means the
vulnerable user's files will be damage, but the administrator can protect the 
damage of system files.
 
Oracle Sun Java fails to properly validate Java applet signatures
2, Not very urgent, because this vulnerability can allow an attacker to modify 
the contents of a signed Java applet without breaking the signature. The dangerous
attach will be the damage of all Java applications.

Apple Safari window object invalid pointer vulnerability
1, Not very urgent. By convincing a victim to view an HTML document (webpage, 
HTML email, or email attachment) with Apple Safari, an attacker could run 
arbitrary code with the privileges of the user running the application. The damage
will be only the running application.

